describe("Create category", () => {
  it("Expect 2 + 2 to be 4", () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it("Expect 2 + 2 to not be 4", () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).not.toBe(result);
  });
});
